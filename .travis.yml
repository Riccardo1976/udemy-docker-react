language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t Riccardo1976/udemy-docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Riccardo1976/udemy-docker-react npm run test
